# config.yaml

hydra:
  run:
    dir: ./logs3/results/${hydra.job.name}
  job:
    name: model:${loss.pretrained_on}_lr:${learning_rate}_bs:${dataloader.batch_size}_same-label:${dataloader.prob_same_label}_only-nir:${dataloader.prob_only_nir}_${now:%m-%d_%H-%M}

defaults:
  - base

model_checkpoint: /Users/robinin/face_recognition/results/model:vggface2_lr:0.001_bs:64_same-label:0.5_only-nir:0.5_01-30_16-10/checkpoints/checkpoint_12960.pt

threshold:
  start: 0.0
  stop: 1.0
  increment: 0.001


model_backbone: r50
distance: 'cosine'
